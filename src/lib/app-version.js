import packageJson from '../../package.json';

// Version de la aplicacion (cod-viewer), no confundir con la version del COD (CODVer, ej. 4.1.1).
export const APP_VERSION = packageJson.version;
export const APP_NAME = 'cod-viewer';
